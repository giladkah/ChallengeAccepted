<h1>Listing order_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @order_types.each do |order_type| %>
  <tr>
    <td><%= order_type.name %></td>
    <td><%= link_to 'Show', [order_type.brand, order_type] %></td>
    <td><%= link_to 'Edit', edit_brand_order_type_path(order_type.brand, order_type) %></td>
    <td><%= link_to 'Destroy', [order_type.brand, order_type], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order type', new_brand_order_type_path(params[:brand_id]) %>
